---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a href={href} class={`text-gray-700 hover:text-black transition-colors font-medium ${className || ''}`}>
  <slot />
</a>
